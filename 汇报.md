# 论文方法汇报稿（Chapter 4）

## 1. 研究问题与动机

本工作面向遥感变化检测中的伪变化问题。伪变化指的是图像在视觉上存在明显差异，但在数据集标注语义中仍被定义为“未变化”的区域。传统方法通常将跨时相视觉差异作为变化判据，在伪变化场景下容易产生较高误检。

因此，本方法的核心目标不是“尽可能检测所有视觉差异”，而是学习与标注语义一致的变化判别能力，即实现 annotation-consistent change detection。

---

## 2. 方法总览

给定双时相输入图像 $\mathbf{I}^{t_1}, \mathbf{I}^{t_2}$，网络前向输出：

- 变化分支 logits：$\mathbf{P}$
- 边缘分支 logits：$\mathbf{P}^{e}$
- 双时相深层特征：$\mathbf{F}^{t_1}, \mathbf{F}^{t_2}$

对应映射为：

\[
\left(\mathbf{P}, \mathbf{P}^{e}, \mathbf{F}^{t_1}, \mathbf{F}^{t_2}\right)
=
\mathcal{N}_{\theta}\left(\mathbf{I}^{t_1}, \mathbf{I}^{t_2}\right).
\]

整体训练由两部分构成：

1. 标签引导的像素级特征对比监督（feature-level contrastive supervision）
2. 有界多任务联合优化（bounded multi-task joint optimization）

---

## 3. 创新点一：标签引导像素级特征对比监督

### 3.1 核心思想

直接在深层特征空间施加标注语义约束，而不仅仅在输出分类空间监督。

- 未变化像素：拉近双时相特征距离
- 变化像素：通过 margin 约束拉开特征距离

### 3.2 关键步骤

先对双时相特征做通道维 L2 归一化：

\[
\tilde{\mathbf{f}}^{t_1}_{ij}=\frac{\mathbf{F}^{t_1}_{ij}}{\|\mathbf{F}^{t_1}_{ij}\|_2}, \quad
\tilde{\mathbf{f}}^{t_2}_{ij}=\frac{\mathbf{F}^{t_2}_{ij}}{\|\mathbf{F}^{t_2}_{ij}\|_2}.
\]

定义像素级距离：

\[
d_{ij}=\left\|\tilde{\mathbf{f}}^{t_1}_{ij}-\tilde{\mathbf{f}}^{t_2}_{ij}\right\|_2.
\]

对比损失（$y_{ij}=0$ 未变化，$y_{ij}=1$ 变化）：

\[
\ell_{\text{con}}^{ij}
=
\frac{1-y_{ij}}{2}d_{ij}^{2}
 \frac{y_{ij}}{2}\max(0,m-d_{ij})^{2}.
\]

图像级聚合并有界化：

\[
\bar{\mathcal{L}}_{\text{con}}=\frac{1}{HW}\sum_{i=1}^{H}\sum_{j=1}^{W}\ell_{\text{con}}^{ij},
\]

\[
\mathcal{L}_{\text{con}}^{\text{norm}}
=
\operatorname{clip}
\left(
\frac{\bar{\mathcal{L}}_{\text{con}}}{\max\left(1,\frac{1}{2}m^{2}\right)},
0,1
\right).
\]

---

## 4. 创新点二：有界多任务联合优化

### 4.1 设计动机

变化分割、边缘监督、对比监督三类损失量纲和数值范围不同，直接加权会导致某一分支主导训练，优化不稳定。

### 4.2 有界归一化与融合

- 分割分支：

\[
\mathcal{L}_{\text{seg}}
=
\alpha \mathcal{L}_{\text{ce}}^{\text{norm}}
(1-\alpha)\mathcal{L}_{\text{iou}}^{\text{norm}},
\]

\[
\mathcal{L}_{\text{ce}}^{\text{norm}}=1-\exp(-\mathcal{L}_{\text{ce}}),
\quad
\mathcal{L}_{\text{iou}}^{\text{norm}}=\operatorname{clip}(\mathcal{L}_{\text{iou}},0,1).
\]

- 边缘分支：

\[
\mathcal{L}_{\text{edge}}^{\text{norm}}=\operatorname{clip}(\mathcal{L}_{\text{edge}},0,1).
\]

- 总目标：

\[
\mathcal{L}_{\text{total}}
=
(1-\beta-\gamma)\mathcal{L}_{\text{seg}}
\beta\mathcal{L}_{\text{con}}^{\text{norm}}
\gamma\mathcal{L}_{\text{edge}}^{\text{norm}},
\]

\[
0\le\beta\le1,\quad 0\le\gamma\le1,\quad \beta+\gamma\le1.
\]

最终有界损失：

\[
\mathcal{L}=\operatorname{clip}(\mathcal{L}_{\text{total}},0,1).
\]

---

## 5. 方法优势总结（汇报可直接讲）

1. 在表示空间引入标签引导对比约束，显式抑制伪变化区域误激活。  
2. 通过有界归一化实现多分支损失尺度对齐，提升训练稳定性。  
3. 将“视觉差异检测”转化为“标注语义一致性检测”，更符合遥感基准评测目标。

---

## 6. 训练流程（一句话版本）

双时相输入经过网络得到变化/边缘预测与双时相特征，分别计算分割损失、边缘损失和标签引导对比损失，各分支有界归一化后按 $\alpha,\beta,\gamma$ 融合，得到最终有界目标并反向传播更新参数。

---

## 7. 建议的现场汇报结构（5-8分钟）

1. 问题背景：伪变化导致误检，传统差异驱动方法存在局限。  
2. 方法总览：网络输出 + 两个核心设计。  
3. 创新一：标签引导像素级特征对比监督（讲清“拉近未变、拉远已变”）。  
4. 创新二：有界多任务联合优化（讲清“先归一化再融合”）。  
5. 总结：方法如何服务 annotation-consistent change detection。

